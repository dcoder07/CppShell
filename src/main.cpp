#include <bits/stdc++.h>
using namespace std;

int main()
{
  cout << unitbuf;
  cerr << unitbuf;

  while (true)
  {
    string input;
    cout << "$ ";
    getline(cin, input);
    cout << input << ": command not found" << endl;
  }

  return 0;
}
